validator: {
    $jsonSchema: {
        bsonType: "object",
        required: [ "name", "comments", "weather", "acreage", "base", "snow_type", "trails", "recent_snowfall" ],
        properties: {
            name: {
                bsonType: "string",
                description: "must be a string and is required"
            },
            comments: {
                bsonType: "array",
                items: {
                    bsonType: "objectId"
                },
                description: "must be an array of objectIds and is required"
            },
            weather: {
                bsonType: "objectId",
                description: "must be an objectId and is required"
            },
            acreage: {
                bsonType: "double",
                description: "must be a double and is required"
            },
            base: {
                bsonType: "double",
                description: "must be a double and is required"
            },
            snow_type: {
                enum: [ "powder", "variable", "groomed", "packed", "spring", "other" ],
                description: "can only be one of the enum values and is required"
            },
            trails: {
                bsonType: "array",
                items: {
                    bsonType: "int"
                },
                minItems: 4,
                maxItems: 4,
                description: "must be an array of ints of length 4 and is required"
            },
            recent_snowfall: {
                bsonType: "double",
                description: "must be a double and is required"
            }
        }
    }
}